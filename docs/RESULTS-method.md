# Method Results

This ledger covers GOAL1 prompt/reasoning experiments. BP35 is the contaminated development game; it is diagnostic evidence, not a held-out result. Its human action baseline is `21/48/44/38/33/87/86/131/163` (651 total).

## Measured Runs

| Method | Model | Turns settled | Actions | Cost | Result | Audit |
| --- | --- | ---: | ---: | ---: | --- | --- |
| No standing prompt | Opus 4.8 | 5 | 6 | $3.5256175 | 0/9, 0.00% RHAE | clean |
| v1 physicist loop | Opus 4.8 | 5 | 5 | $1.426043 | 0/9, 0.00% RHAE | clean |
| v2 falsification | Opus 4.8 | 14 | 28 | $10.926530 | 0/9, 0.00% RHAE | clean |
| v3 state machine | Opus 4.8 | 9 | 15 | $7.700376 | 0/9, 0.00% RHAE | clean |
| v4 affordance, short diagnostic | Opus 4.8 | 5 of 6 started | 6 | at least $1.7059355 | incomplete stream; scorer derives 0/9 | clean |
| v4 affordance, frontier | Fable 5 | 11 | 23 | $42.698812 | 0/9, 0.00% RHAE | clean |
| v5 causal compression | scripted dry stub | 1 | 1 | $0 | current prompt plumbing only; no live evidence | clean |
| v5 causal compression, live | Fable 5 | 5 attempted, 4 committed | 8 | $16.775050 | 0/9; outcome invalidated by turn-5 OAuth 401 | clean |
| v6 surprise ownership, live | Fable 5 | 5 | 7 | $18.327839 | 0/9, 0.00% RHAE | clean |
| v7 local equivariance, live | Fable 5 | 8 across 2 invocations | 15 | $19.694335 | 0/9, 0.00% RHAE | clean |
| v8 observed geometry | scripted dry stub | 1 | 1 | $0 | prompt plumbing only; no live evidence | clean |
| v8 observed geometry, live | Fable 5 | 7 settled + 1 timeout | 17 | at least $28.000493 | 1/9, 2.22% RHAE; L0 cleared in 17 vs human 21 | clean |
| v8 observed geometry, held-out vc33 | Fable 5 | 4 committed + 3 quota errors | 4 | $9.809703 | censored at 0/7; subscription limit before action-4 repair | clean |
| v8 observed geometry, vc33 resumed endpoint | Fable 5 | 8 productive + 3 quota errors across 2 invocations | 9 | $17.747390 | resumed after quota; fixed-turn stop at 0/7, 0.00% RHAE | clean* |
| v9 local repair and matched transfer | scripted dry stub | 1 | 1 | $0 | prompt plumbing only; no live evidence | clean |
| v9 local repair and matched transfer, held-out r11l L0 | Fable 5 | 7 settled + 1 timeout | 10 | $26.093569 | 1/6, 4.76% RHAE; L0 cleared in 10 vs human 22 | clean* |
| v9 held-out r11l continuation, L1 pre-commit | Fable 5 | 6 settled + 3 timeouts + 1 billed no-commit | 8 added (18 total) | $37.345602 continuation; $63.439171 cumulative | 1/6, 4.76% RHAE; L1 replay and six-action plan green but uncommitted | clean* |
| v9 held-out r11l third invocation, L1 clear checkpoint | Fable 5 | 1 | 6 added (24 total) | $4.103890 continuation; $67.543061 cumulative | 2/6, 14.29% RHAE; L1 cleared in 14 vs human 33 | clean* |
| v9 held-out r11l third invocation, L2 renderer checkpoint | Fable 5 | 1 | 7 added (31 total) | $5.647415 continuation; $73.190476 cumulative | 2/6, 14.29% RHAE; a two-pixel renderer mismatch dropped the final four actions of an 11-action L2 plan | clean* |
| v9 held-out r11l third invocation, L2 layer-order checkpoint | Fable 5 | 1 | 2 added (33 total) | $6.538527 continuation; $79.729003 cumulative | 2/6, 14.29% RHAE; a one-pixel compositor-order mismatch dropped the final two L2 actions | clean* |
| v9 held-out r11l third invocation, L2 clear checkpoint | Fable 5 | 1 | 2 added (35 total) | $4.901454 continuation; $84.630457 cumulative | 3/6, 28.57% RHAE; L2 cleared in the 11-action lower bound vs human 51 | clean* |
| v9 held-out r11l third invocation, L3 timeout-race clear checkpoint | Fable 5 | 1 durable commit + driver timeout | 15 added (50 total) | turn unmetered; $84.630457 settled lower bound | 4/6, 47.62% RHAE; L3 cleared in 15 vs human 26 | clean* |
| v9 held-out r11l fourth invocation, L4 overlap checkpoint | Fable 5 | 1 | 1 added (51 total) | $9.701563 continuation; $94.332020 settled lower bound | 4/6, 47.62% RHAE; an 11-cell layer-precedence mismatch dropped the second probe | clean* |
| v9 held-out r11l fifth invocation, L4 repaired-prefix timeout | Fable 5 | 1 timeout | 0 added (51 total) | turn unmetered; $94.332020 settled lower bound | 4/6, 47.62% RHAE; v22 reached `51/51` and a seven-action rollout but did not commit | clean* |
| v9 held-out r11l fifth invocation, L4 absorption/HUD checkpoint | Fable 5 | 1 | 15 added (66 total) | $5.747715 continuation; $100.079735 settled lower bound | 4/6, 47.62% RHAE; absorption held and a one-cell HUD mismatch dropped the final four actions | clean* |
| v9 held-out r11l fifth invocation, L4 clear checkpoint | Fable 5 | 1 | 4 added (70 total) | $6.500947 continuation; $106.580682 settled lower bound | 5/6, 71.43% RHAE; L4 cleared in 20 vs human 52 | clean* |
| v11 inventory and consensus certificates | scripted dry stub | 1 | 1 | $0 | current prompt plumbing only; no live evidence | clean |
| v12 applicability and rollout certificates | scripted dry stub | 1 | 1 | $0 | current prompt plumbing only; no live evidence | clean |
| v13 renderer provenance certificate | scripted dry stub | 1 | 1 | $0 | current prompt plumbing only; no live evidence | clean |
| v14 contextual renderer-precedence certificate | scripted dry stub | 1 | 1 | $0 | current prompt plumbing only; no live evidence | clean |

All prompt hashes match their snapshotted `run.json` values, and the vendored scorer accepts every event stream. The v7 source and frozen run prompt match `bbd457ff…`; the v8 source, dry snapshot, BP35 snapshot, and vc33 snapshot match `934dd71e…`; the v9 source and dry snapshot match `d77045dd…`. The v4 Fable total spans four invocations; two settled turns were subscription-limit errors. Its configured run-cost cap reset on resume, so this run is not clean budget evidence. The short v4 stream lacks final telemetry, making its true cost unprovable. GOAL2 launched a concurrent paid run after the v5 live run began, so v5 is diagnostic rather than concurrency-controlled evidence. V7 waited for the other live runner to exit, but its continuation cap also reset; the final $7.000283 turn exceeded the $5 turn cap and brought the cumulative run $1.694335 above the intended $18 ceiling. V8's first invocation reached $24.656318 against a $22 cap. Its two-turn continuation added $3.344175 of settled telemetry, but GOAL2 launched another runner eight seconds after it began, and its final 1,200-second turn timed out without cost telemetry. The clear is therefore valid game evidence but not concurrency-controlled or exact total-cost evidence.

The first frozen vc33 measurement used a fresh workdir, no released trajectory or environment source, and no overlapping game runner. It produced a contiguous 78-event stream, 21 paired tool calls, four legal actions, no deaths/resets, and exact settled telemetry of $9.809703. The scorer accepted `STOPPED`, 0/7, 0.00% RHAE; the guard was clean. Turn 5 then returned `You've hit your session limit · resets 6:40am`, and two immediate identical refusals triggered the configured no-progress stop. Because only four of the pre-registered 21 actions executed, this is a valid partial trajectory but a censored held-out outcome, not an M1 pass or failure.

The unchanged continuation ran after the provider reset and after the competing runner exited. It added four productive turns, five actions, and $7.937687, producing a combined exact cost of $17.747390. Known Schema runs ended 49 seconds before and began 89 seconds after this continuation, so there was no measured overlap. The continuation restored `4/4` before action 5, then reached `5/5`, `6/6`, and `8/8`; the final action mispredicted and the fixed turn cap ended the run at nine actions, 0/7, and 0.00% RHAE. The guard remained clean, all eight durable turns are complete, and event/action indices are contiguous. One malformed turn-11 `commit_actions` request used `id` rather than `action`, leaving 38 tool starts versus 37 finishes; the corrected request committed exactly once, and scorer, ledger, timeline, and gateway state agree. This logging blemish does not change the result, but the stream is not described as fully paired.

The frozen v9 prompt then reached M1 on untouched `r11l-495a7899`. The fresh, concurrency-isolated run cleared level 0 at action 10 against the 22-action human baseline. The scorer accepts `STOPPED`, 1/6, and 4.76% RHAE; the guard is clean. All 155 event sequence numbers and action indices 0–9 are contiguous, all seven committed ledger turns are `COMPLETE`, and settled telemetry totals $26.093569. The configured $24 cap is checked after a turn, so the $5.206025 final turn overshot it by $2.093569. Turn 5 timed out without telemetry or an action. A malformed turn-6 request used `id` instead of `action`, leaving 54 tool starts versus 53 finishes; its corrected request committed once. The prompt source, dry snapshot, and live snapshot all match `d77045dd…`.

The unchanged second invocation added eight actions and $37.345602 of telemetry, bringing the combined stream to 444 contiguous events, 18 contiguous action indices, and $63.439171. It ended at the continuation cost cap with level 1 still active: the final model backtested `18/18`, and an executable six-action suffix placed the remaining ball on its matched socket in simulation, but the billed final turn exited before `commit_actions`, so no clear is claimed. The scorer still accepts `STOPPED`, 1/6, and 4.76% RHAE; the guard remains clean. Three turns timed out without actions, and a second malformed `id` request was corrected without a duplicate action. The frozen prompt and source still match `d77045dd…`. No other known runner overlapped this invocation; another runner began 37 seconds after it ended.

After that runner exited, the unchanged third invocation began from the same workdir. Its first turn reconfirmed `18/18` and committed the already simulated six-action suffix; all six predictions matched, and action index 23 raised the game to level 2. Level 1 therefore used 14 actions against the 33-action human baseline, making the cumulative level actions `10/14` versus `22/33`. At this checkpoint the scorer accepts `STOPPED`, 2/6, and 14.29% RHAE; the guard is clean, all 461 event sequence numbers and action indices 0–23 are contiguous, and exact telemetry is $67.543061. A third malformed `id` request was corrected before the single six-action commit. The invocation remains active beyond this checkpoint, so later costs and actions are not included in this row.

On its next turn, the same frozen prompt generalized the executable parser from hard-coded colors to the ball colors present in the authoritative gateway snapshot, restored `24/24`, and mechanically certified an 11-action level-2 plan. Seven actions executed before the gate stopped action index 30 on exactly two cells: the model predicted background `5`, while the observation exposed terrain `10` at `(42,27)` and `(42,28)`. The mismatch is consistent with incomplete background restoration beneath a redrawn dotted line; all other pixels and transition flags matched. The four-action suffix was correctly dropped and is not claimed as executed. At this checkpoint events 1–501 and action indices 0–30 are contiguous, the scorer still accepts `STOPPED`, 2/6, and 14.29% RHAE, the guard is clean, and settled telemetry totals $73.190476. The source and live prompt remain byte-identical at `d77045dd…`; turn 21 is active beyond this checkpoint.

Turn 21 confirmed the renderer diagnosis. The two cells were terrain `10` in the structured level-entry observation, became path overlay `1` during actions 24–29, and were wrongly restored to the model's default `5` when action 30 uncovered them. Threading an immutable per-level base layer restored `31/31`; the next transfer matched, and the planned placement changed all causal objects as predicted. Its only mismatch was `(57,55)`, predicted path `1` versus observed socket-arm color `14`: static socket arms composite above paths even when unoccupied. The gate again dropped the suffix, now with two actions remaining. At this checkpoint events 1–549 and action indices 0–32 are contiguous, the scorer accepts `STOPPED`, 2/6, and 14.29% RHAE, the guard is clean, and settled telemetry totals $79.729003. A fourth malformed `id` request was corrected before the single commit, accounting for four unmatched tool starts without a duplicate action.

Turn 22 encoded only the evidenced contextual ordering, restored `33/33`, and committed the two-action remainder. Both predictions matched; action index 34 parked the second ball and raised the game to level 3. Level 2 therefore used exactly 11 actions, matching the independently derived lower bound and beating the 51-action human baseline; per-level actions are now `10/14/11` versus `22/33/51`. At this checkpoint events 1–571 and action indices 0–34 are contiguous, there are no deaths or resets, all three clears occur at indices 9, 23, and 34, and the scorer accepts `STOPPED`, 3/6, and 28.57% RHAE. The guard is clean, the frozen source and snapshot still match `d77045dd…`, and settled telemetry totals $84.630457. The four corrected malformed requests explain 220 tool starts versus 216 finishes without duplicate actions. The original `run.json` retains its first-invocation `max_actions: 22`, while both continuation `run_started` records and the active command use 233; this metadata limitation does not alter the contiguous gateway timeline, but it is disclosed because the scorer displays 233 in its action-limit column.

Turn 23 parsed three independently controlled centroid systems, installed v20 at `35/35`, and committed a 15-action plan. All 15 rendered predictions and flags matched; action index 49 parked the third system and raised the game to level 4. Level 3 therefore used 15 actions against the 26-action human baseline, making the per-level sequence `10/14/11/15` versus `22/33/51/26`. At this checkpoint events 1–605 and action indices 0–49 are contiguous, with no deaths or resets; the scorer accepts `STOPPED`, 4/6, and 47.62% RHAE, the guard is clean, and the frozen source and snapshot still match `d77045dd…`. The commit completed at the 1,200-second boundary: the durable ledger is `COMPLETE` with `executed: 15` and `halt_reason: level_up`, but the driver also classified the killed Claude process as a no-commit timeout and appended `turn_fallback`. No turn-23 telemetry was recovered, so $84.630457 is only the settled cumulative lower bound, not the exact cost. An unrelated runner from the original checkout began during this turn and overlapped approximately its final four minutes; this does not alter the isolated gateway or scorer-accepted actions, but the checkpoint is not concurrency-controlled, fully paired, or exactly costed. Turn 24 started with no tool call or action and was interrupted to restore the one-runner rule; the same frozen workdir will resume only after the other runner exits.

An independent audit using only the structured entry and action observations proves a 13-action mechanics lower bound for level 3: seven distinct controller relocations are necessary, and changing the active controller six times is unavoidable. It also constructs an unexecuted 13-action witness under the observed centroid model. The live plan's exact two-action overhead came from relocating one controller, reselecting another, and then relocating the first again; its motive is not observable. Because the shorter witness was not passed through the live renderer, its exact feasibility remains uncertain, and the result is reported as 15 actions rather than as an optimal clear.

Before any level-4 action, an independent structured audit derived a conditional 16-action witness: two black carrier systems must combine four complementary half-patterns and reach two matching sockets under the centroid rule. The bound is tight only if overlap preserves or absorbs the complementary nonblack regions; that interaction had no historical support. Turn 25 installed v21 at `50/50` and committed a two-action discriminator. Its first action moved one carrier onto a half-pattern and matched everywhere except 11 overlap cells, predicted black `0` versus observed cargo color `8`; the prediction gate dropped the departure probe. This proves that the colored cargo region composites above the carrier in this context, but not whether cargo is absorbed, persists underneath, or remains an independent foreground object. At this checkpoint events 1–633 and action indices 0–50 are contiguous, with no deaths or resets; the scorer accepts `STOPPED`, 4/6, and 47.62% RHAE, the guard is clean, and source and snapshot remain `d77045dd…`. The $9.701563 turn exceeded its $7.50 cap and stopped the invocation; cumulative settled telemetry is $94.332020, still a lower bound because turn 23 was unmetered. An unrelated BP35 runner overlapped the final six minutes, so this checkpoint is not concurrency-controlled.

Two unrelated runners have now launched after a clean process-table preflight, demonstrating that the manual check has a time-of-check/time-of-use gap. The durable tooling gate is a machine-global advisory subscription-runner lock, held across the complete provider invocation and tested for fail-closed contention between separate workdirs. It is proposed rather than implemented on this method-only branch to avoid changing the harness while measuring reasoning; until then, runs remain manually serialized and every overlap is disclosed.

The isolated Turn 26 began after a context rollover and spent 797 seconds before its first tool. It reread only the live model and action-50 history, then copied v21 to v22 at 1,145 seconds, restored `51/51` at 1,168 seconds, and began a structured solve rollout at 1,186 seconds. Seven planned actions reached model-consistent states before action eight triggered an explicit overlap-hazard assertion, but the 1,200-second timeout arrived before any prefix was committed. Events remain contiguous through the timeout, action count stays 51, scorer and guard results are unchanged, and no cost telemetry was recovered. Turn 27 resumes with the replay-green repair installed. This is direct v9 evidence for an anytime-prefix or longer-timeout ablation, but not yet a reason to revise frozen v14: v14 already asks for incremental verified prefixes, and its live behavior remains unmeasured.

Turn 27 reconfirmed `51/51`, completed a 19-action structured rollout, and committed it. The strict departure discriminator and both complementary cargo assemblies matched exactly, supporting absorption rather than transient occlusion. Fifteen actions executed before the pre-declared HUD rival diverged at level-local click index 15: only `(0,17)` differed, predicted filled `5` versus observed empty `0`; all causal and renderer pixels matched, and the four-action parking suffix was dropped. Events 1–681 and action indices 0–65 are contiguous with no deaths or resets, the scorer remains at 4/6 and 47.62% RHAE, and the guard is clean. The isolated turn cost $5.747715, bringing settled telemetry to $100.079735; the cumulative value remains a lower bound because two prior timeouts are unmetered. Turn 28 is active with the replay-green absorption model and the localized HUD repair pending.

Turn 28 localized the mismatch to the HUD's one-shot extra tick at level-local click index 7, installed v23, and restored `66/66`; an in-jail replay of all 16 prior level-4 clicks reproduced the live grid byte-exactly. It then committed the four-action parking suffix, all four predictions matched, and action index 69 raised the game to level 5. Level 4 therefore used 20 actions against the 52-action human baseline, making the per-level sequence `10/14/11/15/20` versus `22/33/51/26/52`. At this checkpoint events 1–737 and action indices 0–69 are contiguous, all five clears occur at indices 9, 23, 34, 49, and 69, and there are no deaths or resets. The scorer accepts `STOPPED`, 5/6, and 71.43% RHAE; the guard is clean, source and live prompt still match `d77045dd…`, and the durable ledger records `COMPLETE`, `executed: 4`, and `halt_reason: level_up`. The isolated turn cost $6.500947, bringing settled telemetry to $106.580682; this remains a lower bound because prior timed-out turns are unmetered. Turn 29 is active on the final level.

V11's scripted smoke produced one legal action and an 11-event scorer-accepted stream. Its source and frozen snapshot both match `f213ec99…`, and the guard is clean. This proves only current prompt wiring; the scripted agent does not exercise either new certificate or provide live performance evidence. V10 (`0a551ed8…`) is the reviewed precursor but has no run entry.

V12's independent scripted smoke has the same one-action, 11-event acceptance profile. Its source and frozen snapshot both match `0ccd433b…`, and the guard is clean. This is also plumbing evidence only.

V13's independent scripted smoke likewise produced one legal action and an 11-event scorer-accepted stream. Its source and frozen snapshot both match `7b598447…`, and the guard is clean. The full suite is 60/60, and structured Codex autoreview reported no actionable findings after the adversarial review fixes. This remains plumbing evidence, not live performance evidence.

V14's final isolated smoke produced one legal action and an 11-event scorer-accepted stream. Its source and frozen snapshot both match `0b242d90…`, and the guard is clean. An adversarial review exposed and then verified fixes for an unsupported-winner loophole and ambiguous isolated-probe comparison; the final structured Codex autoreview reported no actionable findings at 0.91 confidence, and the full suite is 61/61. This remains plumbing evidence, not live performance evidence.

## Bottleneck Diagnosis

- The control and v1 often acted without an executable model.
- V2 modeled earlier but committed to an unsupported “clear everything” goal.
- V3 repeatedly repaired pixels instead of revising its causal representation.
- V4 discovered topology, passive ascent, click affordances, and a replay-green model, but spent heavily reconstructing the renderer. It never used BFS, accumulated eight mispredictions, and remained at level 1 after 23 actions.
- V5 installed its first model after one real probe and immediately backtested each revision green (`1/1`, `4/4`, `5/5`). Its prediction gate stopped all three surprises, so action discipline improved.
- V5 still copied three literal 64×64 frames (12,477 bytes), grew the model from 3.4 KB to 8.3 KB, and carried the full pixel map as latent state. One copied row had 76 cells and needed repair. It inferred coupled click, passive-rise, and camera effects without isolating which changed regions the click caused. No model exposed `is_goal`, and BFS was never called.
- Turn 5 failed with an expired OAuth token after $1.669633 and no action. Therefore 0/9 at eight actions is observed, but the pre-registered no-clear-by-action-21 outcome was not measured.
- V6 explicitly aligned the first surprise in persistent world coordinates, separated sprite, camera, HUD, and world changes, marked one-shot rules provisional, and used single-action probes for undo and click semantics. Its observed click rule was supported by exactly 35 changed world cells rather than already-equal pixels.
- V6 remained inefficient: six 4,159-byte full-grid files, full-map latent state, four mispredictions, and no fully green replay after the first surprise (`3/4` through `5/6`). Models were smaller than v5 (1.7 KB to 5.6 KB), but no model exposed `is_goal`, BFS was never called, and the post-hoc $18 cap overshot by $0.327839.
- V7 fixed the targeted v6 representation error. It maintained persistent world coordinates, inferred a vertical camera offset rather than deleting world rows, and stayed replay-green at `2/2`, `4/4`, `7/7`, `8/8`, and `9/9`. Its first five turns cost $8.957332, 51% less than v6, and its then-current model was 3.4 KB.
- Probe ordering remained wasteful: three executed undo diagnostics delayed the first crate interaction until action 9. Across the full run, 32 queued actions produced 15 executions because six surprises dropped 17 suffix actions.
- On the first large scroll at action 14, v7 correctly aligned the views but saved the observation beside the model instead of seeding its newly revealed rows into initialization. It accepted a `13/14` backtest as a “permanent documented mismatch,” then acted while replay was red. The next click also mispredicted, leaving the final model at most `13/15`; no level cleared and no terminal flag, `is_goal`, or BFS call appeared.
- Local BFS does not search `is_goal`; that function only enables the tool. Search succeeds only when `predict()` emits `level_up` or `win`. A replay-green model can therefore be non-plannable.

The released successful BP35 artifact resolves the reveal-replay ambiguity. Its level-0 snapshot is 10,061 bytes and contains 60 exact observed `LEVEL0_TOP` rows; its final 92,468-byte model contains 1,016 literal map rows. Its notes explicitly describe ingesting observations and seeding newly revealed per-level geometry before returning to a green backtest. Thus v7's blanket large-frame prohibition contradicted the published method on the contaminated development game.

V8 matched that published level-0 representation pattern and passed the development gate:

- The first scroll occurred at action 4. V8 uniquely registered camera offset `-18`, mechanically extracted 18 newly observed static rows, injected them into `init_state`, locally replayed all four transitions with zero diffs, and obtained `4/4` from the harness backtest before acting.
- The next click-and-scroll added 24 more observed rows and returned `5/5`. Subsequent checkpoints were `6/6`, `12/12`, and `13/13`; no reveal was accepted as a permanent exception.
- The level cleared at action 17, four actions below the human baseline and two below the released Fable trajectory. The scorer reports 1/9 and 2.22% RHAE. The contact initially missed only the terminal flag (`16/17`, `#16:level_up`); the timed-out final turn still installed the flag and restored `17/17` before falling back without an action.
- The final 13,180-byte model contains exactly 60 literal observed rows, matching the released level-0 seed count. Across seven committed turns, 25 proposed actions produced 17 executions; six surprises dropped eight suffix actions. No `is_goal` or BFS call appeared.
- Phase latency is now dominant: several repair turns spent long periods before their first tool call, and the level-1 terminal repair exhausted the 1,200-second timeout despite requiring only one evidenced flag. Geometry fidelity no longer explains the stall.

The partial vc33 trace exercised a different failure mode before provider censoring:

- The first model backtested `2/2`; action 3 falsified it. The replacement backtested `3/3` before action 4, preserving the fail-closed replay discipline.
- Action 4 changed 265 cells and disagreed with the model on 264 of them. The subscription limit arrived before the informative transition could be inspected or repaired, so neither predictive-world-model failure nor recovery can yet be judged.
- An independent Codex subscription review (`gpt-5.6-sol` through Omnigent 0.5.1, conversation `conv_fcffb42e9de944a38928bb981a8dd798`) agreed that the run is censored and should resume unchanged with `4/4` replay required before action 5. Its proposed removal of observed-geometry seeding is not adopted: vc33 has shown no camera reveal, so that change would not target the observed mismatch.

The completed vc33 continuation isolated the next bottleneck:

- V3 repaired the 264-cell action-4 surprise immediately and returned to `4/4`. Its conveyor geometry then predicted all five resumed actions exactly, including an unobserved inverse transition and a reduced-change edge regime.
- The only later errors were single HUD cells. Observed increments were `1,2,1,1,1,2,1,1,2`. After the first repeat mismatch, the agent promoted a sparse global rule, “`+2` when global click index is 2 modulo 4,” even though object-local interaction history remained replay-consistent. Two upper-control actions happened to fit; the lower control's second activation falsified the global clock.
- Exact replay and the prediction gate worked: both failed batches stopped after one action. Generalization selection failed. Rejecting one narrow per-button rival did not eliminate the broader matched-control family, and an auxiliary HUD coincidence blocked six otherwise modeled progress actions.
- Turn 10 reached 155,928 context tokens and correctly rolled to a fresh session for turn 11. That final turn spent 514 seconds and $2.944915 planning a seven-action queue; its first one-cell HUD mismatch ended the fixed-turn run before repair.

This supports a local counterexample-repair prior rather than a broader disagreement prompt. [OPINE-World](https://arxiv.org/abs/2607.01531) likewise factors effects by object type, action, and local context, but its published coarse effect signatures discard exact magnitudes and it reports no component ablation. V9 therefore keeps magnitude and adds only matched-object transfer: preserve unaffected rules, prefer typed/local/per-object state over absolute step index or periodicity, and state a held-out prediction before one isolated probe.

This supports the executable-world-model distinction between data fit and plannability described by [Schema Harness](https://schema-harness.github.io/), [WorldCoder](https://arxiv.org/abs/2402.12275), and [Executable World Models](https://arxiv.org/abs/2605.05138).

The clean r11l checkpoint exercises v9's target failure class and exposes its next ceiling:

- A localized overlap surprise was repaired without discarding the midpoint dynamics. The agent transferred a ball-disc rule to an unqueried edge click, stated its coordinate-level prediction, and let the first disagreement halt the suffix. That disagreement exposed the stronger click-anchored rule: the black endpoint moves to the click and the ball is derived as the endpoints' floor midpoint.
- The first far bare-floor placement preserved that causal prediction but stopped on four dotted-line pixels and one HUD pixel. The agent rejected a global clock, assigned the HUD magnitude to local pre-click ownership (`+1` for a sprite hit, `+2` for no sprite hit), and restored `8/8` replay before acting. It recorded the same `+2` prediction for the target placement; level-up replaced the frame, so that held-out magnitude is terminal-censored rather than independently confirmed.
- The causal construction then placed the derived ball into the observed hollow socket and raised level 0 at action 10. No game identifier, source, released trajectory, or game-specific hint entered the method.
- Compliance was still expensive. Four early turns manually transcribed 64×64 prompt frames; two snapshots contained seven wrong row-54 cells and induced a false door rule. Authoritative `read_history` disproved it, but one 1,200-second turn revisited the corrupt files before timing out. A retrospective `3/3` model also lost endpoint identity in a novel overlap state while its prose predicted movement.

The harness already exposes the exact current observation in readable `runtime/gateway_state.json`; raw historical timelines remain jailed, and `read_history(detail="full")` supplies bounded authoritative deltas. This makes the next candidate distribution: unified tool-grounded actual plus prospective rollout 52%, authoritative snapshots alone 26%, prospective rollout alone 13%, shorter checklist 6%, and an action-owner ledger 3%. The unified framing is the smallest change that addresses both measured failures: neither an actual frame nor a predicted suffix should be reconstructed in prose. Conservative action-model learning provides the supporting identifiability principle: only changed pre/post assignments establish effects, while unchanged values merely remain compatible ([Stern and Juba, 2017](https://arxiv.org/abs/1705.08961)).

## Current Experiment Portfolio

Before the v5 live run, the candidate distribution was:

- v5 unchanged causal compression — 35%: measure first to preserve attribution.
- provisional terminal flags plus BFS — 30%: strongest targeted fix if v5 is replay-green but does not search.
- event-conditioned surprise ownership — 15%: use only for repeated patch cycles or replay-green stalls.
- one generic worked example — 10%: higher variance; may improve compliance but risks imitation.
- a shorter imperative/minimal nudge — 10%: cheaper, but earlier terse prompts under-specified goal discovery.

Changes remain sequential. Do not combine the BFS and surprise-routing ablations unless their individual signatures have first been measured.

After v5, evidence shifted the next-change distribution to: identifiability-gated surprise ownership 40%, terminal-flag/BFS coupling 25%, a stronger structural-map rule 20%, a shorter phase checklist 10%, and a worked example 5%. V6 selects only the first. It requires persistent-coordinate alignment, explicit ownership of each changed region, and refuses to generalize from cells that already had the post-action value. The terminal/BFS correction remains untested and separate.

After v6, the distribution shifted again: local-equivariance/coordinate alternatives 37%, terminal-flag/BFS coupling 25%, phase/cost control 18%, raw-literal budgeting 14%, and a worked example 6%. V7 selects only local equivariance. It treats a one-off global shift or deletion of repeated static geometry as a camera-coordinate hypothesis until a matched second transition forces world mutation.

After v7 and inspection of the released BP35 success, the next-change distribution is: retrospective exact geometry seeding 55%, decision-relevant probe ordering 20%, phase/cost control 12%, neighbor-conditioned rule repair 8%, and terminal-flag/BFS coupling 5%. V8 changes only v7's no-frame paragraph. It permits mechanically extracted, observed-only static row or tile seeds, requires persistent-coordinate overlap and dimension assertions, and forbids inventing unseen cells or mixing sprites, HUD, and mutable objects into static seeds. The decisive signature is that a first-scroll `N-1/N` mismatch becomes fully green on the next repair cycle.

V8 produced that signature and a development-game clear, so it remained frozen through the administratively interrupted vc33 continuation. The completed endpoint makes the next distribution: local repair plus matched-object transfer 42%, decision-targeted nuisance gating 22%, magnitude-sensitive typed effect tables 16%, phase/short-batch control 12%, and terminal-flag/BFS coupling 8%. V9 selects only the first. Its decisive signature is that a localized counterexample does not trigger a global clock: notes retain the matched-control prediction, and the first unqueried same-type control transfers without a HUD-only surprise. Generic disagreement is not selected because v8 already maintained and tested rivals; its candidate class, not its willingness to compare, was wrong.

The r11l continuation shifted the next framing before another prompt was measured. Candidate weights were grounded actual-plus-rollout certificates 52%, authoritative actual snapshots alone 26%, prospective rollout alone 13%, a shorter checklist 6%, and an action-owner ledger 3%. V10 selects the unified certificate: the current actual grid comes from the structured gateway and historical deltas from targeted history, while every multi-action suffix is executed through the model or supplied by gateway-aligned BFS before commitment.

That trace then exposed two independent ways for a single executable model to over-certify a batch. Fresh parsing invented a fourth system-12 ring that full history did not contain; later, replay still admitted both position-swap and black-role-transfer semantics, but the first action discriminating them was placed before a queued suffix and produced a 209-cell predicted-vs-actual mismatch. The next distribution was dual-path causal-inventory equality 34%, exact candidate consensus 30%, their combined batching gate 24%, forced single-action play 8%, and a more detailed action-owner ledger 4%. V11 selects the combined gate because it covers both observed failures at the same decision boundary: history-threaded and fresh inventories must agree, and every still-live, materially plausible, replay-consistent, decision-relevant executable candidate must agree on each non-final step. A history-dependent rival that cannot be compared forces one action. No live result is yet attributed to v11.

The next r11l action produced only the base HUD increment where the model predicted a spatial placement, despite exact retrospective replay. This was consistent with an omitted action-applicability or conditional-effect domain while leaving hidden-state and renderer explanations live; the historical replay itself remained exact. The next framing distribution was an executable applicability/precondition guard 36%, mechanical target-footprint support plus branch-complete alternative cost 29%, contextual no-op cause tracking 17%, forcing every novel target to one action 10%, and a shorter checklist 8%. V12 combines the first three into one certificate: multi-action candidates must execute an applicability guard, unsupported actions cannot have a suffix, and a no-op rejects the rendered prediction only in that context while preserving precondition, conditional-effect, hidden-state, and renderer rivals. [Safe Learning of Lifted Action Models](https://arxiv.org/abs/2107.04169) identifies incorrect applicability and incorrect effects as separate causes of failed plans; [SLAF](https://arxiv.org/abs/1401.3437) jointly identifies deterministic preconditions and effects under partial observation. No live result is yet attributed to v12.

The level-2 action-30 gate isolated a different candidate distribution: layered base provenance 44%, suffix-relevant renderer-state consensus 22%, single-action batches whenever an overlay changes 15%, full static-map reconstruction before each suffix 12%, and tolerating renderer-only mismatches 7%. V13 selects layered provenance with only the canonical consensus needed to certify it. It requires an executable cover/uncover/overwrite check, exact provenance for every base value a suffix can expose, and agreement on rendered reveal values while allowing candidates to use different internal caches. The other framings remain untested; tolerating pixel mismatches would violate the exact harness contract. This follows the ordered-layer factorization in [occlusion-aware generative image models](https://arxiv.org/abs/1511.06362) and the use of persistent structured belief under partial observation in [Structured World Belief](https://proceedings.mlr.press/v139/singh21a.html).

Action 32 then showed that base provenance alone is insufficient when two known layers overlap but their precedence is unsupported. The next distribution is an evidence-backed layer-precedence certificate 43%, a single canonical compositor stack 25%, matched overlap probes 14%, forcing one action at every novel overlap 11%, and renderer-equivalence tolerance 7%. The live repair preserved the now-proven base layer, changed only the contextual precedence rule, restored exact replay, and cleared the level in the independently derived action lower bound. V14 therefore selects the evidence-backed certificate: each distinguishable winner needs a mechanically re-evaluable history or invariant witness, unsupported precedence stops the suffix, and isolated probes receive exact post-observation comparison. Its dry validation is separate from live performance evidence.

The level-3 checkpoint produced a different distribution: measure frozen v14 unchanged 42%, add anytime verified-prefix discipline 30%, increase the operational timeout 15%, add a structural lower-bound or dominance certificate 9%, and require symbolic BFS 4%. No v15 prompt change is selected. Both the observed 15-action clear and the unexecuted 13-action witness beat the human baseline of 26 and therefore receive the same capped per-level RHAE; extra optimization has no current score value. The turn was latency-bound but correct: its first tool began 559 seconds after start, the replay-green model arrived after 1,020 seconds, the durable commit completed at 1,191 seconds, and every action matched before the 1,200-second fallback. V14 is already 2,218 words versus v9's 654 and has no live result, so adding proof work now would confound attribution. [Anytime A*](https://papers.nips.cc/paper_files/paper/2003/hash/ee8fe9093fbbb687bef15a38facc44d2-Abstract.html) supports improving an already feasible incumbent, while [Time-Bounded A*](https://www.ijcai.org/Proceedings/09/Papers/079.pdf) assumes safely explorable state spaces that irreversible ARC actions do not guarantee, and [online dominance pruning](https://ojs.aaai.org/index.php/AAAI/article/download/10663/10522) warns that contextual equivalence does not establish global dominance. A narrow anytime-prefix rule remains the next candidate only if a v14 run actually times out while holding a certified progress prefix; a longer driver timeout is an operational ablation, not a reasoning improvement.

The level-4 overlap recurrence gives the next distribution: retain v14's contextual precedence and existing surprise-owner split 51%, add an explicit interaction-versus-occlusion identifiability gate 24%, prescribe a matched departure probe 13%, force single actions at all novel composite overlaps 7%, and encode a semantic cargo/opacity prior 5%. No new prompt change is selected. V14 already requires mechanically witnessed contextual precedence, exact post-observation comparison, and preservation of causal alternatives after a renderer surprise; the failed v9 pair behaved safely because the gate dropped its suffix. The next real observation, not a prompt assumption, must distinguish persistence from absorption.

## Status and Uncertainty

M0 and M1 are proven. The contaminated development gate passes, and frozen v9 has cleared the first five untouched r11l levels in `10/14/11/15/20` actions while restoring exact retrospective replay before every winning suffix. M2 is not yet satisfied: the same live trajectory is active on level 5 with one level remaining. The next required evidence is a full clean-game clear with the same frozen method, followed by two untouched-game clears under one final frozen method before any generalization or ~99% reproduction claim.
